{
  "name": "playwright.cucumber.js.test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "pretest": "npx ts-node support/init.ts",
    "test": "cross-env ENV=test cucumber-js test",
    "test:report": "yarn pretest & cross-env ENV=test cucumber-js test --retry 0 & cross-env ENV=test npx ts-node support/report.ts",
    "test:rerun:report": "cross-env ENV=test cucumber-js test & cucumber-js -p rerun @rerun.txt & npx ts-node support/report.ts"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^9.6.0",
    "@playwright/test": "^1.38.1",
    "@types/node": "^20.8.3",
    "cross-env": "^7.0.3",
    "dotenv": "^16.3.1",
    "fs-extra": "^11.1.1",
    "multiple-cucumber-html-reporter": "^3.5.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2",
    "winston": "^3.11.0"
  }
}
